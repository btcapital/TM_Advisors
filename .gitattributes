# Normalise line endings so the repo stays consistent between Windows checkouts
# and the Linux runners that GitHub Pages and Actions use.
* text=auto eol=lf

# Never let git mangle binaries
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.woff  binary
*.woff2 binary
